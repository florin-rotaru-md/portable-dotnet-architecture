# Fill in IPs from `terraform output` after provisioning.
# Use the external (LAN) IPs for Ansible SSH access.

[k3s_server]
prod-server-1 ansible_host=192.168.1.21 ansible_user=ops

[postgres]
prod-db-1     ansible_host=192.168.1.22 ansible_user=ops

[k3s:children]
k3s_server

[all:vars]
ansible_ssh_private_key_file=~/.ssh/id_ed25519
