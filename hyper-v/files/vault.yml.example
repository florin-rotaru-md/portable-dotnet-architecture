---
# Copy this file to native/infra/ansible/inventory/group_vars/all/vault.yml
# Encrypt with: ansible-vault encrypt vault.yml

ansible_ssh_public_key: "{{ lookup('file', '~/.ssh/id_ed25519_ansible.pub') }}"

postgres_password: "change-me-strong-password"

# Required only for private Git repositories used by the first auto-deploy
# app_repo_token: "ghp_xxx"

# Required only when use_cloudflared: true
# cloudflare_token: "eyJhIjoiABCD..."