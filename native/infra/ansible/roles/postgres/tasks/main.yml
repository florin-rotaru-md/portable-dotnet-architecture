- name: Validate required postgres secrets
  assert:
    that:
      - postgres_password is defined
      - postgres_password | string | trim | length > 0
      - postgres_password != 'change-me-strong-password'
    fail_msg: >-
      Define postgres_password in inventory/group_vars/all/vault.yml before
      running bootstrap. Start from inventory/group_vars/all/vault.yml.example and
      encrypt the file with ansible-vault if needed.
  no_log: true

- name: Add PostgreSQL apt signing key
  get_url:
    url:  https://www.postgresql.org/media/keys/ACCC4CF8.asc
    dest: /usr/share/keyrings/postgresql.asc
    mode: '0644'

- name: Add PostgreSQL apt repository
  apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/postgresql.asc] http://apt.postgresql.org/pub/repos/apt {{ ansible_distribution_release }}-pgdg main"
    state:    present
    filename: pgdg

- name: Install PostgreSQL and PostGIS
  apt:
    name:
      - "postgresql-{{ postgres_version }}"
      - "postgresql-{{ postgres_version }}-postgis-3"
    state:        present
    update_cache: true

- name: Configure pg_hba.conf (local md5 auth)
  template:
    src:   pg_hba.conf.j2
    dest:  "/etc/postgresql/{{ postgres_version }}/main/pg_hba.conf"
    owner: postgres
    group: postgres
    mode:  '0640'
  notify: restart postgresql

- name: Ensure PostgreSQL is enabled and started
  service:
    name:    postgresql
    enabled: true
    state:   started

- name: Determine target PostgreSQL databases
  set_fact:
    postgres_target_databases: "{{ postgres_databases | default([postgres_db]) | unique }}"

- name: Check if app DB user exists
  become_user: postgres
  command: psql -tAc "SELECT 1 FROM pg_roles WHERE rolname='{{ postgres_user }}'"
  register: pg_user_check
  changed_when: false

- name: Create app DB user
  become_user: postgres
  command: >
    psql -c "CREATE USER {{ postgres_user }}
             WITH PASSWORD '{{ postgres_password }}' CREATEDB"
  when: pg_user_check.stdout != '1'
  no_log: true

- name: Check if app database exists
  become_user: postgres
  command: psql -tAc "SELECT 1 FROM pg_database WHERE datname='{{ item }}'"
  loop: "{{ postgres_target_databases }}"
  loop_control:
    label: "{{ item }}"
  register: pg_db_check
  changed_when: false

- name: Create app database
  become_user: postgres
  command: >
    psql -c "CREATE DATABASE {{ item.item }}
             OWNER {{ postgres_user }}"
  loop: "{{ pg_db_check.results }}"
  loop_control:
    label: "{{ item.item }}"
  when: item.stdout != '1'

- name: Enable postgis extension on app database
  become_user: postgres
  command: psql -d {{ item }} -c "CREATE EXTENSION IF NOT EXISTS postgis"
  loop: "{{ postgres_target_databases }}"
  loop_control:
    label: "{{ item }}"
  changed_when: false

- name: Ensure backup directory exists
  file:
    path:  /opt/postgres/backups
    state: directory
    owner: postgres
    group: postgres
    mode:  '0750'

- name: Install backup cron job
  cron:
    name:   "postgres nightly backup"
    user:   postgres
    minute: "15"
    hour:   "2"
    job:    >
      for db in {{ postgres_target_databases | join(' ') }}; do
      pg_dump -Fc "$db" > /opt/postgres/backups/${db}_$(date +\%Y\%m\%d_\%H\%M).dump;
      done
      && find /opt/postgres/backups -name '*.dump' -mtime +7 -delete
