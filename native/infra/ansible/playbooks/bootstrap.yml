---
- name: Bootstrap single-app VPS (native .NET)
  hosts: app
  become: true
  roles:
    - common
    - dotnet
    - postgres
    - nginx
    - app
    - role: cloudflared
      when: use_cloudflared | bool
