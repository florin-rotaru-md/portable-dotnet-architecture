---
- name: Install base packages
  apt:
    name:
      - curl
      - git
      - wget
      - unzip
      - jq
      - ufw
      - ca-certificates
      - gnupg
      - lsb-release
      - fail2ban
      - htop
    state: present
    update_cache: true

- name: Allow SSH through UFW
  ufw:
    rule:  allow
    port:  "22"
    proto: tcp

- name: Enable UFW
  ufw:
    state: enabled

- name: Enable fail2ban
  service:
    name:    fail2ban
    enabled: true
    state:   started

- name: Set up SSH authorized key for {{ ansible_user }}
  authorized_key:
    user:  "{{ ansible_user }}"
    state: present
    key:   "{{ ansible_ssh_public_key }}"
  when: ansible_ssh_public_key is defined
